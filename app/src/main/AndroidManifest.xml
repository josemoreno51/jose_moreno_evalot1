<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.pps">

    <!-- ========== PERMISOS SIMPLIFICADOS ========== -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- SOLO UNO de estos permisos de almacenamiento -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <!-- ========== APPLICATION ========== -->
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.MaterialComponents.Light.DarkActionBar"
        android:requestLegacyExternalStorage="true"
        tools:ignore="UnusedAttribute">

        <!-- Login Activity (Main) -->
        <activity
            android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- View Noticia Activity -->
        <activity
            android:name=".ViewNoticiaActivity"
            android:exported="false" />

        <!-- Create Account Activity -->
        <activity
            android:name=".CreateAccountActivity"
            android:exported="false" />

        <!-- Forgot Password Activity -->
        <activity
            android:name=".ForgotPasswordActivity"
            android:exported="false" />

        <!-- Home Activity -->
        <activity
            android:name=".HomeActivity"
            android:exported="false" />

        <!-- Add Noticia Activity -->
        <activity
            android:name=".AddNoticiaActivity"
            android:exported="false" />

        <!-- Noticia Detail Activity -->
        <activity
            android:name=".ViewNoticiaActivity"
            android:exported="false" />

    </application>

</manifest>